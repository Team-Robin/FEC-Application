import React from 'react';
import PropTypes from 'prop-types';

const FILL_ME_IN = ({ PROP1, PROP2, PROP3, PROP4, PROP5 }) => (

);

FILL_ME_IN.propTypes = {
  PROP1: PropTypes.FILLIN.isRequired,
  PROP2: PropTypes.FILLIN.isRequired,
  PROP3: PropTypes.FILLIN.isRequired,
  PROP4: PropTypes.FILLIN.isRequired,
  PROP5: PropTypes.FILLIN.isRequired,
};

export default FILL_ME_IN;
